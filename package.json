{
  "name": "alisveris-sokagi",
  "version": "1.0.0",
  "private": true,
  "description": "Alışveriş Sokağı — Dünya Turu",
  "scripts": {
    "yayin": "node scripts/yayin.js",
    "sync": "node scripts/yayin.js && npx cap sync",
    "ios": "node scripts/yayin.js && npx cap sync ios && npx cap open ios",
    "android": "node scripts/yayin.js && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
